# Raw extraction source data and build scripts — kept locally only, not published.
# Mirrors HEE's own setup: the public site repo ships the built dashboard
# (docs/index.html, app.js, data/*.json), not the raw pipeline inputs/scripts.
docs/dex_hff/
